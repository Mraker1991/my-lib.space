document.querySelectorAll('.switch__btn-form').forEach(function(tabsBtn){  tabsBtn.addEventListener('click', function(e){    const path = e.currentTarget.dataset.path;    document.querySelectorAll('.switch__btn-form').forEach(function(btn){      btn.classList.remove('switch__btn-form--active')});    e.currentTarget.classList.add('switch__btn-form--active');    document.querySelectorAll('.form-section').forEach(function(tabsBtn){      tabsBtn.classList.remove('form-section--active')});    document.querySelector(`[data-target="${path}"]`).classList.add('form-section--active');  });});